language: python
python:
  - 2.7
  - 3.6
  - 3.8
install:
  - pip install pipenv
  - pipenv install
  - python setup.py install
  - pip install pytest pytest-cov
  - pip install codecov
script:
  - pytest --cov
after_success:
  - codecov